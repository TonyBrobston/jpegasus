{
  "name": "jpegasus",
  "version": "1.9.54",
  "description": "A client-side JavaScript tool that takes in a `File` of type `.jpeg`, `.png`, or `.gif` and returns a compressed `File` of type `.jpeg`.",
  "main": "./lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/TonyBrobston/jpegasus"
  },
  "scripts": {
    "build": "rm -rf lib && tsc",
    "version": "yarn build && git add .",
    "postversion": "git push && git push --tags",
    "coverage": "jest --coverage",
    "lint:js": "eslint --fix ./src ./tests",
    "test": "yarn coverage && yarn lint:js",
    "unit": "npx jest"
  },
  "keywords": [
    "javascript",
    "image",
    "jpeg",
    "gif",
    "png",
    "client",
    "client-side",
    "compression",
    "compressor",
    "canvas",
    "scale",
    "size",
    "reduce",
    "smaller"
  ],
  "author": "Tony Brobston",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/chance": "^1.0.1",
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.0",
    "chance": "^1.0.18",
    "eslint": "^5.14.1",
    "eslint-config-google": "^0.12.0",
    "jest": "^24.1.0",
    "ts-jest": "^24.0.1",
    "ts-node": "^8.0.3",
    "typescript": "^3.4.1"
  },
  "peerDependencies": {
    "core-js": "^3.0.0",
    "regenerator-runtime": "^0.13.2"
  }
}
